paths:
  /book:
    post:
      tags:
        - Book
      operationId: create
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '../components/book.yaml#/schemas/BookRequest'
      responses:
        200:
          description: Model was created
          content:
            application/json:
              schema:
                $ref: '../components/book.yaml#/schemas/Book'
